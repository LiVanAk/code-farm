from util import *

directions = ['North', 'East', 'South', 'West']

def dfs():
    pass
 
def start(x, y, n):
    move_to(x, y)
    while True:

        if get_entity_type == 'Treasure':
            harvest()
